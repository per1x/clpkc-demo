# 充电桩配置（key=value，# 为注释）。可用同名环境变量覆盖：
#   CLPKC_PILE_ID / CLPKC_CLOUD_HOST / CLPKC_CLOUD_PORT /
#   CLPKC_SHARED_KEY_HEX / CLPKC_CONNECT_TIMEOUT_MS / CLPKC_READ_TIMEOUT_MS

pile.id = pile-001
cloud.host = 127.0.0.1
cloud.port = 9000

# 与云平台的全局预共享密钥（64 字符 hex）。生产环境请通过环境变量注入，勿提交真实值。
shared.key.hex = 00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff

connect.timeout.ms = 3000
read.timeout.ms = 15000
